# deploy/kubernetes

Optional Kubernetes/HPA manifests for proactive scaling (§27 of the spec). Kubernetes is
explicitly NOT required for the core project — this directory stays empty unless that
stretch goal is picked up after the core milestones are done.
